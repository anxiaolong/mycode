61.为什么要使用对象克隆？
@：
创建一个对象比较麻烦或者需要保留原对象情况下，需要使用对象克隆。

62.如何实现对象克隆？
@：
实现Cloneable接口，并重写Object类的clone()方法。
实现Serializable接口，通过对象的序列化和反序列化实现克隆，可以实现真正的深度克隆。

63.深克隆和浅克隆区别时什么？
@：
浅克隆只是复制了引用地址，修改一个对象的属性值，另一个也会改变。
深克隆是将对象的值也全部复制过来，修改一个对象的属性值另一个不会改变。

