79.http响应码301和302有什么区别？
@：
301和302都表示链接发生了转移。
301表示链接发生了永久转移，302表示暂时转移。

80.forward和redirect的区别？
@：
直接转发和间接转发。

81.tcp和udp的区别？
@:
tcp需要先建立连接，udp不需要建立连接。
tcp传递数据更可靠，udp传递效率更高。
tcp需要更多系统资源，udp相对需要更少。

82.tcp为什么要三次握手而不是两次？
@：
tcp通信双方都需要维护一个序列号用来表示数据包，
如果没有第三次握手，无法确定接收方是否受到了数据。

83.tcp粘包是怎么产生的？
@：
发送方发送的数据包大小很小时，多个数据包会放入到发送缓冲区，此时产生粘包。
接收方接收数据时候，缓冲区存放数据的速度大于应用层读数据的速度时，产生粘包。

84.osi的七层模型都有哪些？
@：
应用层：网络服务与最终用户的一个接口。
表示层：数据的表示、安全、压缩。
会话层：建立、管理和终止会话。
传输层：定义传输数据的协议端口号，以及流控和差错校验。
网络层：进行逻辑地址寻址，实现不同网络之间的路径选择。
数据链路层：建立逻辑连接，进行硬件地址寻址、差错校验等功能。
物理层：建立、维护、断开物理连接。

85.get和post有哪些区别？
@：
get在浏览器回退时时无害的，post会再次提交请求。
get产生的url地址可以被bookmark，post不可以。
get的请求会被浏览器主动缓存，post不会。
get智能进行url编码，post支持多种编码。
get请求参数会被浏览器保留，post不保留。
get的url中传递参数有长度限制，post没有。
get智能接受ascii字符，post没有限制。
get不安全，参数暴露在url上，敏感信息传递不用get。
get参数通过url传递，post方request body中。












